import { SafeAreaView, Text } from "react-native";
export default function MapTab() {
  return (
    <SafeAreaView
      style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
    >
      <Text style={{ color: "#64748b" }}>Map coming tomorrow…</Text>
    </SafeAreaView>
  );
}
